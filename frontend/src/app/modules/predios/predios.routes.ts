import { Routes } from '@angular/router';
import { PredioListComponent } from './components/predio-list.component';

export const PREDIOS_ROUTES: Routes = [
  {
    path: '',
    component: PredioListComponent
  }
];
























